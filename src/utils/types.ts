export interface PlayListItemModel {
  name : string;
  coverImgUrl : string;
  description : string;
  playCount : number;
};

export interface MusicItemModel {
  name : string;
  picUrl : string;
  id : number;
}